[project]
id = "machete"
version = "0.2.0"
mach = "^6.0"
src = "src"
out = "out/{target.name}/{profile.name}"

[target.windows-x86_64]
isa = "x86_64"
os  = "windows"
abi = "win64"

[target.linux-x86_64]
isa = "x86_64"
os  = "linux"
abi = "sysv64"

[target.linux-aarch64]
isa = "aarch64"
os  = "linux"
abi = "aapcs64"

[target.darwin-x86_64]
isa = "x86_64"
os  = "darwin"
abi = "sysv64"

[target.darwin-aarch64]
isa = "aarch64"
os  = "darwin"
abi = "aapcs64"

[artifact.machete]
default = true
kind = "bin"
entry = "root.mach"
out = "bin/machete{artifact.suffix}"
targets = ["*"]
link = ["kernel32"]
need = []

# decodes Leela training data (binpack) into training records; see src/binpack.mach
[artifact.binpack]
kind = "bin"
entry = "tools/binpack.mach"
out = "bin/binpack{artifact.suffix}"
targets = ["*"]
link = ["kernel32"]
need = []

[link.kernel32]
source = "system"
name = "kernel32.dll"
os = ["windows"]
isa = ["*"]
abi = ["*"]
export = false

[profile.debug]
default = true
opt = 0
debug = true
simd = "scalarize"
vectorize = false
float_reassoc = false

[profile.release]
opt = 2
debug = false
simd = "scalarize"
vectorize = true
float_reassoc = false

[dep.std]
git = "https://github.com/briar-systems/mach-std"
version = "^9.0"
